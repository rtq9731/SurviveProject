# 챕터 1 완성 — 자율 작업 게이트

- 작성일: 2026-08-02
- 목표: **프롤로그부터 챕터 1 종료까지 사람 개입 없이 한 번에 플레이 가능한 상태**
- 선행 문서: `docs/superpowers/specs/2026-08-02-prologue-chapter1-design.md`, `docs/superpowers/specs/2026-08-02-ecosystem-design.md`

이 문서는 **읽기 전용 스펙**이다. 여기 없는 기능은 만들지 않는다.
스펙이 틀렸다고 판단되면 고치지 말고 **정지 + 로그**한다.

---

## 0. 이 문서가 존재하는 이유

2026-08-02 작업에서 드러난 패턴이 있다.

| 자율로 잘 굴러간 것 | 반드시 사람이 필요했던 것 |
|---|---|
| 코드 작성·리팩터·테스트 | 세계관 오독 ("여기 지구임") |
| 컴파일 에러 진단·수정 | 범위 결정 ("프롤로그는 불시착·이동만") |
| 에셋 배치·NavMesh·프리팹 | 설계 방향 ("산소는 물에서만") |
| 프로그램적 상태 검증 | **플레이 감각** ("제작대에 레시피가 없다") |

마지막 줄이 핵심이다. 놓친 버그 셋 — 제작 레시피 미표시, 홀드 게이지 미표시, 물에서 탈출 불가 — 은 **전부 "성공" 보고 뒤에 사람이 발견했다.** 프로그램적으로는 통과했지만 실제로 플레이하면 안 되는 것들이었다.

그래서 이 게이트의 1번 목표는 **그 구간을 자동화하는 것**이다.

---

## 1. 절대 규칙

1. **이 문서를 수정하지 않는다.** 틀렸으면 정지 + 로그.
2. **`[사람]` 게이트를 스스로 통과 처리하지 않는다.** 도달하면 즉시 멈추고 `CHAPTER1_LOG.md`에 적은 뒤 기다린다.
3. **게이트 하나 통과할 때마다 커밋한다.** 커밋 메시지는 게이트 ID로 시작한다: `A2: 챕터1 목표 6단계 E2E 통과`
4. **재시도 2회.** 3회째에 막히면 자력 해결을 포기하고 정지 + 로그.
5. **게이트는 순서대로.** 건너뛰지 않는다. 막히면 우회하지 말고 멈춘다.
6. **범위 밖을 만들지 않는다.** 챕터 2 콘텐츠, 도감 UI, 소비자급 생물, 건축 — 전부 금지.

---

## 2. 오늘 배운 것 — 반복하면 안 되는 실수

전부 실제로 당한 것이다. 규칙으로 박아둔다.

### 2.1 ScriptableObject·MonoBehaviour는 클래스 하나당 파일 하나

한 파일에 여러 개를 넣으면 Unity가 `m_Script` 참조를 연결하지 못한다.
- ScriptableObject: 에셋이 `m_Script: {fileID: 0}`으로 저장되어 로드 실패
- MonoBehaviour: `The referenced script on this Behaviour is missing` — **더 엄격하다**

두 번 당했다. `ObjectiveSO.cs`(SO 4개), `ObjectiveTriggers.cs`(MonoBehaviour 3개).

### 2.2 UI 스크립트는 자기 자신이 붙은 오브젝트를 비활성화하지 않는다

`Awake`에서 `gameObject.SetActive(false)`를 하면, 나중에 `SetActive(true)`가 **그 Awake를 처음 깨우고** 곧바로 다시 닫아버린다. 영영 열리지 않는다.

두 번 당했다. `SubtitleView`, `CraftingUI`.

**보이고 안 보이고는 `CanvasGroup`의 `alpha`·`blocksRaycasts`·`interactable`로만 다룬다.**

### 2.3 에셋을 재생성하면 GUID가 바뀌어 참조가 죽는다

삭제 후 재생성한 에셋을 참조하던 프리팹·씬은 **조용히 null이 된다.** 컴파일도 통과하고 에러도 안 난다.

`RecipeBook.asset`을 재생성한 뒤 `CraftingUI.book`이 null이 되어 레시피가 하나도 안 떴다.

**에셋을 재생성했으면 반드시 전수 점검한다.** (게이트 D1의 도구가 이 일을 한다)

### 2.4 씬 저장은 `MarkSceneDirty` + `SaveScene(scene)`

`EditorSceneManager.SaveOpenScenes()`는 디스크에 쓰지 않는 경우가 있다. 성공을 반환하고도 파일이 그대로였다.

### 2.5 동적 코드 제약

- 게임 타입은 `using`으로 **직접 참조**한다. `System.Type.GetType`은 보안 정책에 막힌다
- `AssetDatabase.DeleteAsset`, `System.IO.*` 금지 — 파일 삭제는 터미널로
- `SerializedObject`로 값을 넣은 뒤 **`EditorUtility.SetDirty`** 를 부른다

### 2.6 상호작용 콜라이더는 겉모습보다 넉넉하게

팩 프리팹에 콜라이더가 없거나(양치류) 0.2m로 작으면(생성 모델) 3m 밖에서 조준이 불가능하다. **최소 지름 1.1m**를 보장한다.

### 2.7 테스트 하네스 자체를 의심한다

플레이어 회전을 코드로 바꿔도 `PlayerCameraRig`가 매 프레임 `_yaw`로 덮어쓴다. "탐지 실패"가 게임 버그가 아니라 테스트 방식 문제였다.

**카메라가 실제로 보는 방향**을 기준으로 검증한다.

---

## 3. 현재 상태 (게이트 시작점)

커밋 `c721e064` 기준.

**동작하는 것**
- 이동·점프·시점, 수영(Dry/Wading/Swimming), 물속 산소 소모
- 인벤토리(15칸, Tab 토글), 제작(레시피 5종 표시), ESC 닫기·일시정지
- 채집(홀드 + 진행 게이지), 도구 장착(Q 순환, F 랜턴)
- 랜턴 배터리·발광 버섯 군락 충전, 목표 6단계 순차 진행
- 생태계 순환(식물 성장·생산자 섭취·분해자 회수)
- 자막 시퀀스, HUD(체력·산소·스크랩·배터리·목표)

**검증 수단**
- EditMode 테스트 77개
- `uloop compile` / `run-tests` / `screenshot` / `simulate-keyboard`

**아직 안 된 것**
- 프롤로그 → 챕터 1 씬 전환
- 저장/불러오기 실사용
- 밸런스 전반 (전부 추정치)
- Feel 피드백 내용 (연결만 되어 있고 비어 있음)
- 콘텐츠가 물가에 뭉쳐 있음

---

## 4. 게이트

### A. 검증 기반 — 이것부터 세운다

가장 먼저 하는 이유: 이후 모든 게이트의 통과 판정이 여기에 걸린다.

#### A1 [자동] E2E 하네스
플레이 모드에서 **실제 입력으로** 게임을 조작하고 상태를 검증하는 재사용 도구.

만들 것: `Assets/09.Tests/PlayMode/` 아래 헬퍼
- 플레이어를 지정 위치·방향으로 세우기 (`PlayerCameraRig`의 `_yaw`를 직접 설정해야 한다 — 2.7 참조)
- 카메라 정면에 대상을 두고 조준시키기
- 키 입력 재생 (누르기·홀드·떼기)
- 상태 단언 (인벤토리 수량, 목표 인덱스, 플래그, UI 열림 여부)

**통과 기준**
- 하네스로 "곡괭이 줍기 → 인벤토리에 1개" 를 처음부터 끝까지 자동 검증
- 실패 시 어느 단계에서 왜 실패했는지 로그에 남는다

#### A2 [자동] 챕터 1 목표 6단계 E2E
A1 하네스로 목표를 **실제 조작으로** 하나씩 통과시킨다. 플래그를 코드로 세우는 것은 통과로 치지 않는다.

| 목표 | 실제 조작 |
|---|---|
| 1 착지 이탈 | 트리거까지 걸어간다 |
| 2 버섯 군락 | 군락 광원 지대까지 걸어간다 |
| 3 스크랩 10개 | 흩어진 잔해를 홀드 채집한다 |
| 4 곡괭이 제작 | 제작대에서 버튼을 누른다 |
| 5 랜턴 제작 | 제작대에서 버튼을 누른다 |
| 6 포탈 기동 | 재료를 모아 포탈과 상호작용한다 |

**통과 기준**
- 여섯 단계가 전부 실제 조작으로 완료되고 `ChapterCompleted`가 발생한다
- 콘솔 에러 0건
- 실행 시간이 기록된다 (밸런스 판단의 근거가 된다)

#### A3 [자동] 참조 무결성 점검 도구
2.3의 재발을 막는다.

만들 것: 씬·프리팹의 `[SerializeField]` 참조 중 **null인 것을 전수 보고**하는 에디터 도구.

**통과 기준**
- 도구가 MainScene·StartScene·주요 프리팹을 훑고 null 참조 목록을 낸다
- 현재 프로젝트에서 실행했을 때 **의도된 null만 남는다** (의도된 것은 목록에 근거를 적는다)

---

### B. 콘텐츠 마감

#### B1 [자동] 배치 재조정
현재 콘텐츠가 물가에 뭉쳐 있다. 배치 방향을 +Z로 잡았는데 그쪽이 호수였고, 36개를 마른 땅으로 옮기면서 분포가 뭉쳤다.

**통과 기준**
- 채집 노드·식물·생물이 **부유섬 위와 육지에 고르게** 분포한다 (한 곳에 3개 이상 겹치지 않는다)
- 목표 동선(착지 → 군락 → 채집 → 제작대 → 포탈)이 한 방향으로 이어진다
- 물속에 잠긴 콘텐츠 0개
- A2 E2E가 여전히 통과한다

#### B2 [자동] 프롤로그 → 챕터 1 씬 전환
지금은 두 씬이 따로 논다.

**통과 기준**
- StartScene에서 동굴 진입 트리거를 밟으면 암전 후 MainScene으로 넘어간다
- 넘어간 뒤 챕터 1 목표 1이 표시된다
- 프롤로그 자막이 재생되고 끝난다

#### B3 [자동] 저장·불러오기
`SaveService`가 만들어져 있으나 실사용되지 않는다.

**통과 기준**
- 체크포인트에서 저장 → 게임 재시작 → 인벤토리·목표 진행도가 복원된다
- E2E로 검증한다

---

### C. 밸런스와 감각 — 여기부터 사람이 필요하다

#### C1 [사람] 조명 밝기
지하는 어두운 것이 맞지만, **랜턴을 얻기 전에 이동이 가능한 밝기인지**는 눈으로 봐야 안다.

정지하고 물어볼 것:
- 랜턴 없이 초반 탐색이 가능한가, 아니면 답답한가
- 발광 버섯 군락이 "안전한 밝은 곳"으로 읽히는가

#### C2 [사람] 수치 밸런스
전부 추정치다. A2가 기록한 실행 시간을 근거로 제안하되, **판단은 사람이 한다.**

| 항목 | 현재 값 |
|---|---|
| 채집 시간 | 흩어진 잔해 1.4초 / 기계 잔해 2초 |
| 랜턴 배터리 | 최대 100, 1.6/초 소모, 스크랩 1개당 20 회복 |
| 식물 성장 | 재양치 40초 / 불씨버섯 55초 |
| 생산자 섭취 | 6초 간격, 배부름 12 |
| 수영 산소 | 6/초 소모, 20/초 회복 |
| 포탈 요구 | 스크랩 15 + 외계 합금 2 |

정지하고 물어볼 것: 챕터 1 목표 플레이 시간과 각 수치의 체감

#### C3 [사람] Feel 피드백 조립
`MMF_Player` 필드는 12곳에 연결되어 있으나 **내용이 비어 있다.** 무엇을 넣을지는 감각의 영역이다.

정지하고 물어볼 것: 어느 지점을 가장 강하게 만들 것인가 (설계 4.15에서는 산소 경고를 꼽았으나 산소가 상황 자원으로 바뀌었으므로 재검토가 필요하다)

---

### D. 마무리

#### D1 [자동] 전체 회귀
**통과 기준**
- EditMode 테스트 전부 통과
- A2 E2E 통과
- A3 참조 점검 통과
- 컴파일 에러 0, 경고는 목록화

#### D2 [사람] 최종 플레이
사람이 처음부터 끝까지 직접 플레이한다. 이 게이트만 통과하면 챕터 1이 끝난 것이다.

---

## 5. 범위 밖 (만들면 위반)

- 챕터 2 콘텐츠 — 등불버섯·거울잎 나무·숲·평야
- 소비자급 생물 (랩터형·다관절형·거미형)
- 도감 UI·스캐너
- 건축, 낮/밤, 매크로늄 채굴
- 편의 기능 — 자동 조준, 미니맵, 퀘스트 마커, 설정 화면
- 새 무기 종류
- 멀티플레이어

기능을 추가하고 싶어지면 그건 대부분 위반이다.

---

## 6. 정지 규약

`[사람]` 게이트에 도달했거나 재시도 2회에 실패했으면 `CHAPTER1_LOG.md` 맨 아래에 한 줄 append하고 **작업을 중단한다.**

```
| 게이트 | 막힌 내용 | 사람이 준 답 | 자율 재개 |
|---|---|---|---|
| C1 | (한 줄) | | |
```

"사람이 준 답" 칸은 비워둔다. 사람이 채운다.
**정지 후 다른 게이트로 우회하지 않는다.**

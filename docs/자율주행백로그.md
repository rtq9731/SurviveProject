# 자율 주행 백로그

사용자 부재 중 AI가 자율적으로 수행할 수 있는 작업 목록. 2026-08-05 코드베이스 전수 조사 기반.

## ✅ 소진 완료 (2026-08-05 자율 주행 6라운드, main `51fb3042`)

자율 실행 가능 항목 전부 완료: **1~7, 9~10, 12~19, 21~25** + 사용자 추가 배정(20 점검, 21 벽 통과, UIManager·프리팹 분리). 11은 이미 구현되어 있음이 확인됨, 8은 참조 발견으로 사용자 확인 대기.
- EditMode 테스트 **123 → 432** (전부 통과), E2E 시나리오 9종 통과 (신규 4종 포함)
- 결함 수정 6건: 자가 타격, 생물 벽 통과(몸+경로), 비행 무한 상승, 비행 천장 관통, isStopped 고착, 트리거 안 조준 실패
- 남은 것은 아래 "자율 실행 금지 — 사용자 확인 필요" 항목뿐
- 인프라 제안(미구현): E2E를 병렬 게이트로 쓰려면 InputSystem `backgroundBehavior=IgnoreFocus` 항구화 검토 (다중 에디터 포커스 문제, R6-A 보고 참조)

## 운영 규칙

1. **게이트 필수** — 모든 작업은 AI가 스스로 성공/실패를 판정할 수 있는 게이트를 갖는다. 게이트를 통과하지 못하면 완료 보고 대신 실패 보고를 한다. 사용자는 사후 보고만 받는다.
2. **충돌 금지** — 서로 충돌하지 않는 작업만 동시에 다룬다. 같은 파일·같은 씬·같은 Domain 폴더를 건드리는 작업은 병렬 배정하지 않고 순차 처리한다. 각 항목의 `영역` 태그가 겹치면 충돌로 간주한다.
3. **병렬 인프라** — `E:\SurviveProjectClones\A·B·C` 클론 리포지토리에서 각각 별도 Unity 인스턴스로 uloop를 사용한다. 각 클론의 헤드 에이전트는 Opus 5.0 권장.
4. **회수 방식** — 클론에서 완료한 작업은 브랜치/커밋으로 본 리포지토리(`E:\SurviveProject`)에 회수한다. 커밋 메시지는 기존 규약(한국어 서술형, 무엇을 왜)을 따른다.
5. **금지 구역** — 서드파티 에셋(`Feel`, `polyperfect`, `Stylized Water`, `Unvik_3D`, `TextMesh Pro`), 자동 생성 코드(`PlayerInputActions.cs`), 미커밋 상태의 씬/프로젝트 설정(`TestScene.unity`, `EditorSettings.asset`, `TimeManager.asset`, `ChosunGu SDF.asset`)은 건드리지 않는다.

## 공통 게이트 절차

모든 작업은 최소한 다음을 순서대로 통과해야 완료로 간주한다.

1. `uloop-clear-console` → `uloop-compile` — 에러 0, 신규 경고 0
2. `uloop-run-tests` (EditMode) — 기준선 전부 통과 + 신규 테스트 통과 (기준선: R1 전 123 → R4 시점 **394**. 최신 수는 직전 실행 기록을 따른다)
3. 참조 무결성·아트 규칙 점검 — R4부터 `AutonomousGate.Run()` 한 번으로 (`execute-dynamic-code --code 'Survive.EditorTools.AutonomousGate.Run();'`) — `{"pass":true, brokenReferences:0, artViolations:0}` 확인. (개별 메뉴 `Tools/Survive/*`도 유지)
5. 해당되는 경우 E2E 시나리오(`E2ERunner.Status` 폴링) 통과
6. `git diff` 검토 — 변경이 의도한 파일 범위를 벗어나지 않음

---

## Tier 1 — 순수 Domain 테스트·구현 (위험 낮음, 게이트 완전)

### 1. 환경 위협 판정(깊이 게이팅) Domain 구현 + 테스트 ★최우선
- **내용**: P2 스펙(`docs/superpowers/specs/2026-08-04-p2-depth-gating-design.md` §8-1)의 첫 항목. 어떤 장비로 어떤 구역을 통과할 수 있는가를 `Survive.Domain`에 순수 함수로 구현.
- **게이트**: 스펙 §9가 이미 정의 — "환경 위협 판정 EditMode 테스트: 장비별 통과 가부, 경계값". 신규 테스트 통과 + 기존 123 유지.
- **영역**: `Domain\`(신규 폴더) + `09.Tests\EditMode\`(신규 파일)
- **위험**: 낮음. 스펙에 설계까지 명시됨. HANDOFF.md §4가 "원격 가능"으로 분류.

### 2. ItemStack 경계값 테스트
- **내용**: `Domain\Items\ItemStack.cs`의 `IsEmpty`, `RemainingSpace`, `Clear` 경계값 테스트 신규 작성.
- **게이트**: 신규 테스트 통과 + 기존 123 유지.
- **영역**: `09.Tests\EditMode\ItemStackTests.cs`(신규만, 프로덕션 코드 무변경)
- **위험**: 매우 낮음.

### 3. PlacementCheck.Describe enum 전수 테스트
- **내용**: `Domain\Building\PlacementCheck.cs`의 `PlacementCheckText.Describe()`가 enum 8종 전부에 비어 있지 않은 문자열을 돌려주는지 전수 검사. switch 누락 시 UI에 거부 사유가 안 뜨는 결함 유형(P1에서 실제 발생)의 재발 방지.
- **게이트**: 신규 테스트 통과 + 기존 123 유지.
- **영역**: `09.Tests\EditMode\`(신규 파일만)
- **위험**: 매우 낮음.

### 4. RecipeBookSO / BuildCatalogSO 무결성 테스트
- **내용**: `Domain\Crafting\RecipeBookSO.cs`, `Domain\Building\BuildCatalogSO.cs`의 조회 로직과 중복 ID 검출 테스트.
- **게이트**: 신규 테스트 통과 + 기존 123 유지.
- **영역**: `09.Tests\EditMode\`(신규 파일만)
- **위험**: 매우 낮음.

### 5. SnapGraph 테스트
- **내용**: `Building\SnapGraph.cs`의 `TryFindNearest` 반경 경계, null 정리 로직 테스트. 정적 상태이므로 `SetUp`/`TearDown`에서 `Clear()` 호출로 테스트 간 오염 차단. (Assembly-CSharp 소속이면 순수 부분의 Domain 이동이 선행 — 이동 시 이 항목은 Tier 4-13과 영역 충돌 주의)
- **게이트**: 신규 테스트 통과 + 기존 123 유지 + 컴파일 통과.
- **영역**: `Building\SnapGraph.cs` + `09.Tests\EditMode\`
- **위험**: 중간(정적 상태 격리).

### 6. SaveService 순수 로직 Domain 추출 + 직렬화 왕복 테스트
- **내용**: `Core\SaveService.cs`(113줄)는 Assembly-CSharp 소속이라 EditMode 테스트가 참조 불가. 직렬화/역직렬화 순수 부분을 `Domain\Core\`로 추출하고 왕복(round-trip) 테스트 작성. 세이브는 현재 EditMode 테스트 0인 핵심 시스템.
- **게이트**: 컴파일 통과 + 신규 테스트 통과 + 기존 123 유지 + **E2ESave 시나리오 통과**(동작 불변 증명).
- **영역**: `Core\SaveService.cs`, `Domain\Core\`, `09.Tests\EditMode\`
- **위험**: 중간(리팩터링 포함).

## Tier 2 — Dead code 제거 (위험 낮음)

### 7. Narrative\ScreenFader.cs 삭제
- **내용**: `02.Scripts\Narrative\ScreenFader.cs`(51줄)는 어디서도 참조되지 않는 중복 클래스. 실제 사용되는 것은 `02.Scripts\UI\ScreenFader.cs`. 삭제 전 참조 0 재확인(grep + 씬/프리팹 GUID 검색).
- **게이트**: 컴파일 통과 + 참조 무결성 점검 0 + 기존 123 유지.
- **영역**: `Narrative\`
- **위험**: 낮음.

### 8. EasingFunctions.cs 삭제 — ~~자율 실행~~ → **사용자 확인 필요로 이동 (R1에서 판명)**
- **정정(2026-08-05 R1)**: "미참조 dead code"라는 전제가 사실과 다름. `CameraShake.cs`(:22, :42, :44)가 컴파일 타임 의존하고, 그 `CameraShake`는 `05.Prefabs\Player.prefab`에 컴포넌트로 붙어 있다.
- **정리하려면**: `EasingFunctions.cs` + `CameraShake.cs` + `Player.prefab` 컴포넌트 제거를 한 묶음으로 — 프리팹을 만지므로 씬·프리팹 배타 라운드 대상이고, `CameraShake`가 기획상 쓰일 예정인지 사용자 판단 필요.

## Tier 3 — P2 기능 구현 (게이트 있음, 설계 판단 일부 포함)

### 9. 액면 보행 장비 (P2 §8-2)
- **내용**: 제작 레시피 추가, 장착 처리, 환경 위협 판정과 연결. **항목 1 완료 후 순차 진행**(같은 Domain 영역 — 병렬 금지).
- **게이트**: 신규 EditMode 테스트(레시피 조회·장착 판정) + 기존 전체 유지 + 컴파일 통과. 가능하면 신규 E2E 시나리오.
- **영역**: `Domain\`(위협 판정 인접), `Crafting\`, `08.Data\`
- **위험**: 중간. HANDOFF.md가 "원격 가능"으로 분류.

### 10. 사망 드롭과 회수 (P2 §8-3)
- **내용**: 사망 시 인벤토리 드롭, 시체/가방 회수로 왕복 압박 완성. HANDOFF.md가 "원격 가능"으로 분류.
- **게이트**: **신규 E2E 시나리오 작성이 게이트의 일부** — 사망→드롭 생성→회수→인벤토리 복원까지 E2E로 검증. + 기존 EditMode·E2E 전체 유지.
- **영역**: `Inventory\`, `Vitals\`, `Testing\`(신규 시나리오), Domain 일부
- **위험**: 중간~높음(설계 여지). 시작 전 스펙 §8-3을 정독하고, 스펙이 모호한 지점은 보수적으로 선택 후 보고서에 결정 사항 명기.

### 11. 물속 Shift=달리기, Ctrl=하강 — **이미 구현됨 (R4 확인, 2026-08-05)**
- `PlayerLocomotion.cs:145-157`에 Shift=수영 가속(swimSprintFactor)·Ctrl=하강(IsDescending)이 구현되어 있고 E2EBaseBuilding 어서션("보통 3.2m/s → Shift 4.6m/s", "Ctrl 1.5초에 3.9m 하강")으로 검증되고 있다. CHAPTER1_LOG의 "미처리" 기록이 낡은 정보였음. 작업 불필요.

### ~~11(구). 원래 항목~~ (CHAPTER1_LOG 후속 요청 ①)
- **내용**: `CHAPTER1_LOG.md` 최하단 미처리 요청. `Player\PlayerSwimming.cs`/`PlayerLocomotion.cs` 입력 매핑 변경.
- **게이트**: E2E 입력 시뮬(`uloop-simulate-keyboard`)로 Shift 홀드 시 수평 속도 증가, Ctrl 홀드 시 수직 속도 음수를 수치로 측정 + 기존 수영 E2E 통과.
- **영역**: `Player\`, `Input\`, `Testing\`
- **위험**: 중간. 항목 13과 영역 충돌 — 병렬 금지.

## Tier 4 — 코드 품질 (동작 불변, E2E로 증명)

### 12. BuildPlacer 겹침 판정 상수 정리
- **내용**: `Building\BuildPlacer.cs:268,279`의 `OverlapSphereNonAlloc(…, 1.0f)`과 `Distance(…) > 0.6f` — 서로 관계있는 두 반경이 이름 없는 상수로 흩어져 있음. 명명 상수화(+ `:182`의 `0.4f` 여유 높이). **수치 자체는 변경하지 않는다.**
- **게이트**: 컴파일 통과 + EditMode 전체 + **E2EBaseBuilding 시나리오 15개 전부 통과**(동작 불변 증명).
- **영역**: `Building\BuildPlacer.cs`
- **위험**: 중간.

### 13. PlayerLocomotion 튜닝 상수 명명화
- **내용**: `Player\PlayerLocomotion.cs:157`의 부력 수렴 `3.5f`, `:183,188`의 SphereCast 반경 `0.7f` 중복 등을 명명 상수/SerializeField로. 수치 불변.
- **게이트**: 컴파일 통과 + EditMode 전체 + 수영/이동 관련 E2E 통과.
- **영역**: `Player\`
- **위험**: 낮음~중간. 항목 11과 영역 충돌 — 병렬 금지.

## Tier 5 — 검증 인프라 확장 (게이트를 생산하는 작업)

### 14. E2E 시나리오 커버리지 확장
- **내용**: 현재 E2E 시나리오 7종 + E2EBaseBuilding 15종. 커버되지 않은 흐름(수영 왕복, 채집→제작→건축 연결, 산소 고갈 사망) 시나리오 추가.
- **게이트**: 신규 시나리오가 현재 빌드에서 통과 + **의도적 실패 주입 시 실패함을 확인**(테스트의 테스트) 후 원복.
- **영역**: `Testing\`(신규 파일)
- **위험**: 낮음(프로덕션 코드 무변경). 단 항목 10·11이 Testing\을 만지므로 파일 단위로 분리.

### 15. CreatureBrain 의사결정 로직 Domain 추출 + 테스트
- **내용**: 생물/전투는 테스트 0인 유일한 주요 시스템. `Creatures\CreatureBrain.cs`(244줄)의 상태 전이·판단 로직 중 순수 부분을 Domain으로 추출하고 테스트 작성.
- **게이트**: 컴파일 통과 + 신규 테스트 통과 + 기존 123 유지 + PlayMode에서 생물 행동 로그 무에러(`uloop-get-logs`).
- **위험**: 중간~높음(동작 불변을 E2E로 완전 증명하기 어려움 — PlayMode 관찰 + 스크린샷 보조).
- **영역**: `Creatures\`, `Combat\`, `Domain\`(신규), `09.Tests\EditMode\`

### 19. FlyerMotor 지면 레이캐스트 자기충돌 수정 (R3에서 발견된 선행 결함)
- **내용**: `Creatures\FlyerMotor.cs`의 `GroundHeight()`가 `pos + up*50`에서 아래로 쏘는 광선이 자기 콜라이더를 먼저 맞아 `desired.y = 자기 위치 + 2.5m`가 되고, 비행 생물 4마리가 초당 약 8.8m로 무한 상승한다. 게임 시작 20초면 플레이어가 닿을 수 없는 높이(실측 t=20s에 y≈228). 수정: 레이 시작점 조정 또는 `groundMask`에서 생물 레이어 제외.
- **게이트**: 컴파일 + EditMode 전체 유지 + **PlayMode 고도 실측** — 비행 생물이 60초 이상 거처 높이 ±hoverHeight 근방(예: y 50~60)에 머무름을 프레임 스텝으로 측정. `get-logs` 에러 0.
- **영역**: `Creatures\FlyerMotor.cs`
- **위험**: 낮음.

### 20. 플레이어 자가 타격 가능 여부 점검 (사용자 직접 배정, 2026-08-05)
- **내용**: 플레이어의 공격(곡괭이 등)이 플레이어 자신을 때릴 수 있는지 점검. 코드 경로(공격 판정의 대상 필터)와 PlayMode 실험 양쪽으로 확인하고 보고. 수정은 점검 결과 보고 후.
- **게이트**: 코드 근거(파일:라인) + PlayMode 재현 시도 결과(체력 변화 수치)로 결론 입증.
- **영역**: `Combat\`(읽기), PlayMode 실험
- **위험**: 없음(조사만).

### 22. 자가 타격 수정 (20번 점검 결과 확정된 결함, 2026-08-05)
- **내용**: `Combat\MeleeSwing.cs` 판정에서 가해자 자기 제외(해석된 IDamageable의 root 비교) + `PlayerDamageReceiver.TakeDamage`에 `info.Source` 자기 루트 무시 이중 방어. 원뿔·자기제외 판정 순수부를 `Domain\Combat`으로 추출해 EditMode 테스트. 발치 광맥(54.1°) 채굴 중 자해가 재현 조건.
- **게이트**: 컴파일 + EditMode 전체 + 신규 Domain 테스트 + PlayMode 재현 실험 역검증 — 아래 70° 스윙 후 HP 불변, 발치 광맥 채굴 시 HP 불변, 생물 공격은 여전히 들어옴(피격 경로 생존 확인).
- **영역**: `Combat\`, `Domain\Combat\`(신규), `09.Tests\EditMode\`
- **위험**: 낮음.
- **보류 제안(기획 판단)**: OverlapSphere에 가시선 검사가 없어 얇은 벽 너머 대상 타격 가능 — 별도 결정 필요.

### 21. 생물체 벽 통과 해결 (사용자 직접 배정, 2026-08-05)
- **내용**: 생물체가 벽(건축물·지형)을 통과하는 문제 수정. 이동 방식(transform 직접 이동 vs 물리) 조사 후 코드로 해결. 씬·프리팹 수정 금지 제약 내에서.
- **게이트**: 컴파일 + EditMode 전체 유지 + PlayMode 실측 — 벽을 사이에 두고 생물 유인, 통과 불가 확인(좌표 로그) + 기존 배회·추격 동작 유지 + get-logs 에러 0.
- **영역**: `Creatures\`
- **위험**: 중간.

### 23. 건축물에 NavMeshObstacle 부여 (R5-C 후속 제안)
- **내용**: 생물 벽 통과는 CreatureCollisionGuard로 몸은 막았지만, NavMeshAgent의 *경로*는 여전히 벽을 관통해 그려져 생물이 벽에 붙어 밀거나 가장자리로 돈다. 근본 해결: 건축물 스폰 시 `NavMeshObstacle`(carving) 부여 — 프리팹 수정 또는 BuildPlacer 스폰 코드에서 AddComponent.
- **게이트**: 컴파일 + EditMode 전체 + E2E 건축 시나리오 유지 + PlayMode 실측(벽 뒤 목표에 대해 생물이 벽을 향하지 않고 우회 경로를 그리거나 포기).
- **영역**: `Building\` (또는 건축물 프리팹 — 프리팹 배타 규칙 적용)
- **위험**: 중간 (carving은 NavMesh 성능·재계산 부하 고려).

### 24. CreatureBrain.StopMoving의 isStopped 미복원 점검·수정 (R5-C 발견)
- **내용**: `CreatureBrain.cs:236` `StopMoving()`이 `agent.isStopped = true`만 하고 되돌리는 곳이 없어 보인다. 실제로 이동 재개가 막히는지 재현 확인 후, 결함이면 상태 전이 시 복원 추가.
- **게이트**: 재현 스크립트(멈춤 유발 후 배회/추격 재개 관찰) + 컴파일 + EditMode 전체 + 기존 행동 관찰 유지.
- **영역**: `Creatures\CreatureBrain.cs`
- **위험**: 낮음.

### 25. 비행 생물 천장 관통 (R5-C 잔여)
- **내용**: CreatureCollisionGuard는 수평 전용이라 비행 생물이 바위 천장을 세로로 뚫는 경우가 남아 있다. FlyerMotor 고도 갱신에 상향 캐스트 추가 검토.
- **게이트**: PlayMode 실측(천장 아래 유지) + FlyerMotor 기존 고도 유지 게이트(R4-C의 124초 유계) 재확인.
- **영역**: `Creatures\FlyerMotor.cs`
- **위험**: 낮음~중간.

### 16. 게이트 자동화 스크립트
- **내용**: 참조 무결성 점검·아트 규칙 점검·EditMode 테스트를 한 번에 실행하고 결과를 JSON/텍스트로 남기는 배치 진입점(예: `Editor\AutonomousGate.cs` 정적 메서드). 이후 모든 자율 작업의 게이트 실행이 한 줄이 된다.
- **게이트**: 스크립트 실행 결과가 현재 기준선(위반 0, 끊김 0, 123 통과)과 일치.
- **영역**: `Editor\`(신규 파일)
- **위험**: 낮음.

## Tier 6 — UI 관리 체계 (사용자 직접 배정, 2026-08-05)

> 배경: 현재 UI 전체(HUD·PanelInven·CraftingPanel·StoragePanel·QuickSlotBar·ObjectivePanel·PanelDialog·SubtitleLine·BuildPlacementPrompt 등)가 `05.Prefabs\CvsUI.prefab` 단일 프리팹(281KB)에 들어 있고, MainScene 인스턴스에 다량의 오버라이드가 걸려 있다. UI 하나만 고쳐도 모든 UI 작업이 같은 파일에서 충돌한다. `UI\UIStateService.cs`(ESC 스택·일시정지)와 `IClosablePanel`이 이미 존재하므로 UIManager는 **이를 대체하지 말고 확장**한다 (평행 시스템 이중화 금지).

### 17. UIManager 시스템 구축
- **내용**:
  - 패널 열림/닫힘 스택 로직(현재 `UIStateService`의 `_openOrder` 관리)의 순수 부분을 `Domain\UI\`로 추출 — 열림 순서, 최후 열림 우선 닫기, 전체 닫기 규칙을 MonoBehaviour 없이 테스트 가능하게.
  - 패널 자기등록 API — 현재의 "씬 전체 `FindObjectsByType` 스캔" 폴백을 패널 `OnEnable` 시 자기등록으로 대체(프리팹화 이후에도 배선이 안 끊기는 구조의 전제).
  - 레이어/포커스 규칙 — 동시에 열릴 수 있는 패널 조합(HUD는 상시, 전체화면 패널은 상호 배타 등)을 데이터로 정의.
- **게이트**: 컴파일 통과 + **Domain 추출분 EditMode 테스트 신규**(스택 순서·배타 규칙 경계값) + 기존 123 유지 + PlayMode에서 각 패널 열기/ESC 닫기/일시정지 흐름을 `uloop-simulate-keyboard`+`uloop-get-logs`로 검증(에러 0) + 기존 E2E 전체 유지.
- **영역**: `UI\`, `Domain\UI\`(신규), `09.Tests\EditMode\`
- **위험**: 중간. 18과 같은 영역 — **18보다 먼저, 순차로**.

### 18. UI 프리팹 분리 (CvsUI 해체)
- **내용**: 단일 `CvsUI.prefab`을 패널 단위 중첩 프리팹으로 분리 — 최소 단위: HUD(바이탈·산소·배터리·힌트), PanelInven, CraftingPanel, StoragePanel, QuickSlotBar, ObjectivePanel, PanelDialog+SubtitleLine, BuildPlacementPrompt. 씬 오버라이드는 가능한 한 프리팹 안으로 흡수해 오버라이드 수를 최소화. 이후 UI 작업은 패널별 프리팹 파일만 건드리게 되어 병렬 충돌이 구조적으로 사라진다.
- **게이트**:
  1. 컴파일 + 참조 무결성 점검 0 + EditMode 123 유지
  2. `uloop-execute-dynamic-code`로 구조 검증 — 각 패널이 독립 프리팹 에셋으로 존재하고, 씬/CvsUI의 해당 노드가 프리팹 인스턴스이며, 씬 오버라이드 수가 분리 전보다 감소했음을 프로그램적으로 확인
  3. **분리 전/후 Game View 스크린샷 비교**(`uloop-screenshot`) — HUD·각 패널 열림 상태가 픽셀 수준에서 동일 구도
  4. 기존 E2E 전체 통과(UI 경유 시나리오 포함) + PlayMode 로그 에러 0
- **영역**: `05.Prefabs\`(UI), **`01.Scenes\MainScene.unity`**, `UI\`
- **위험**: 높음(씬+대형 프리팹 재구성). **씬 배타 규칙**: 이 작업이 도는 라운드에는 어떤 클론에도 씬·프리팹을 만지는 작업을 배정하지 않으며, 시작 전 미커밋 씬 변경이 정리된 상태여야 한다. 17 완료 후 진행.

---

## R7 — 사용자 결정 반영 (2026-08-05 결정 수신)

사용자 결정: ① 리스폰 = 마지막 화톳불, 없으면 시작점 ② 드롭 가방 세이브 유지 ③ 벽 너머 타격 금지(가시선 검사) ④ EasingFunctions+CameraShake 묶음 삭제 승인("아마 삭제해도 될듯" — 실사용 발견 시 중단) ⑤ 물속 15m 익사 밸런스 현행 유지.

### 26. 부활·리스폰 (결정 ①)
- 사망 시 마지막(가장 최근에 세운/불붙인) 화톳불에서 부활, 화톳불이 없으면 시작 지점. 바이탈 회복 규칙 포함. 드롭 가방은 죽은 자리에 남는다(기존 동작).
- **게이트**: EditMode 432 유지 + Domain 신규 테스트 + 신규/확장 E2E — 사망→화톳불 부활→가방 회수 왕복, 화톳불 없을 때 시작점 부활.
- **영역**: `Vitals\PlayerVitals.cs`, 신규 리스폰 서비스, `Domain\`. **DeathDropService·DeathDropBag 수정 금지**(27과 충돌 방지).

### 27. 드롭 가방 세이브 지속 (결정 ②)
- 저장 후 재시작해도 가방이 위치·내용물 그대로 복원. 런타임 스폰 오브젝트 복원 경로 신설.
- **게이트**: EditMode 432 유지 + E2ESave 확장(가방 있는 채 저장→재적재→복원 확인) + 기존 E2EDeathDrop 유지.
- **영역**: `Inventory\DeathDropBag.cs`, `Vitals\DeathDropService.cs`, 세이브 연동. **PlayerVitals 수정 금지**(26과 충돌 방지).

### 28. 벽 너머 타격 차단 (결정 ③)
- MeleeSwing에 가시선(occlusion) 검사 — 시전 지점과 대상 사이가 구조물·지형으로 막혀 있으면 제외.
- **게이트**: EditMode 432 유지 + PlayMode 실측(벽 너머 대상 타격 불가 대조군/실험군, 개활지 타격 정상, 채굴 정상) + E2E 유지.
- **영역**: `Combat\MeleeSwing.cs`, `Domain\Combat\`

### 8(재개). EasingFunctions+CameraShake 묶음 삭제 (결정 ④)
- `Utill\EasingFunctions.cs` + `CameraShake.cs` + Player.prefab의 CameraShake 컴포넌트 제거. **선행 확인**: CameraShake를 호출하는 코드가 있으면(착지 충격파 등) 삭제 중단하고 보고.
- **게이트**: 컴파일 + EditMode 432 유지 + 참조 무결성 0 + E2E 스모크.
- **영역**: `Utill\`, `CameraShake.cs`, **`05.Prefabs\Player.prefab`** — 이번 라운드 프리팹 배타 단독.

## R8 — 이동을 위협으로 (2026-08-05 사용자 승인) — ✅ 완료 (main `c39b91ac`, EditMode 523)

29(매크로늄 접촉)·30(소비자 "낫" + 빛 회피 + 랜턴 포인트 전환)·31(낙하 대가) 전부 병합. 남은 것: §8-4 지형 다듬기 때 균열·액면·낫 서식지 씬 배치 (코드는 전부 준비됨).


배경: 현 군도 지형에서 "가장자리 낙하"는 사문화(해안이 완만히 수면 아래로), 섬 1~3 사이 물이 완전히 안전. 사용자 승인 방향: ① 매크로늄 접촉 대가 ② 소비자 1종 ③ 어둠이 감추는 균열 지형(§8-4에서 배치, 코드 절반은 지금).

### 29. 매크로늄 접촉 대가
- 장비 없이 액면에 닿으면 사망 — 기존 사망 드롭·리스폰 루프에 연결. 씬 배치는 §8-4와 함께, 컴포넌트·판정·E2E(런타임 스폰 구역)까지.
- **게이트**: EditMode 467 유지 + 신규 테스트 + E2E(접촉→사망→드롭→리스폰, 장비 보유 시 무해).
- **영역**: `World\`, `Domain\World\`, `Testing\`

### 30. 소비자 1종 (§8-5)
- 섬4의 유일한 포식자. Aggressive 성향(선공·시야 감지) Domain 추가, 정의 SO·스폰 가능 프리팹(신규, 블록아웃 수준 — 실루엣 "뾰족하다=도망쳐라"), 씬 배치는 §8-4와 함께.
- **추가 결정(2026-08-05)**: **소비자는 빛을 피한다** — 밝은 구역(LitZoneRegistry, 랜턴 점등 포함)에 들어가지 않고, 추격 중에도 빛 경계에서 멈추거나 물러난다. 빛이 방어 수단 = 배터리가 생존줄이라는 주제 연결.
- **랜턴 형태 확정(2026-08-05)**: 랜턴은 **포인트 라이트**(플레이어 주변 전방위 웅덩이) — 방어 반경도 시선 무관 전방위. 판정 반경은 lampLight의 기본 range 기준(경고 깜빡임에 요동치지 않게).
- **게이트**: EditMode 467 유지 + Domain 전이 테스트 + E2E(스폰→감지→추격→공격→플레이어 사망 루프 연결, 도주 성립).
- **영역**: `Creatures\`, `Domain\Creatures\`, `05.Prefabs\`(신규만 — 이번 라운드 프리팹 배타), `08.Data\`

### 31. 낙하 대가 (③의 코드 절반)
- 경착지(바위 등 고체) 시 낙하 속도 기반 피해/사망. **물 입수는 무해 유지.** 균열 배치는 §8-4에서.
- **게이트**: EditMode 467 유지 + Domain(속도→피해 곡선) 테스트 + E2E(높은 곳→바위 착지 피해/사망, 물 입수 무해, 일상 점프 무해).
- **영역**: `Player\PlayerLocomotion.cs`, `Domain\`, `Testing\`

## R9 — 월드 상호작용 (2026-08-05 브레인스토밍 확정)

### 32. 매크로늄 바다 — 헤엄치는 동안 피해
- 섬 사이 액체 = 묽은 매크로늄 (스펙 §4 개정 참조). 잠수·수영 중 지속 피해, Wading 무해. 피해량은 실측 조율: 맨몸 1→2 횡단이 체력 25~35% 소모, Shift가 실질 생존기. 낙하 물 입수 무해는 유지하되 입수 후 DoT 시작.
- **게이트**: EditMode 전체 + Domain(피해율 규칙) 테스트 + E2E — 횡단 비용 실측, Wading 무해, 익사·DoT 사망 모두 드롭·리스폰 루프 연결, E2ESwimRoundTrip 개정 통과.
- **영역**: `Vitals\`, `Player\PlayerSwimming` 연동부, `Domain\`, `Testing\`

### 33. 발광 버섯 갓 루프
- 갓 채집(홀드) → 군락 빛 갓 수 비례 감소 → 전부 따면 소등·LitZoneRegistry 제외(낫 진입 가능) → 수 분 후 재생. 아이템 `glow_cap`(스펙 §3 섬2 자원). 퀵슬롯 사용으로 발밑 배치 = 약한 길표시 빛(**LitZoneRegistry 미등록 — 낫 못 막음**), E로 회수. 군락 부착은 런타임 자기 설치(씬 무수정). 제작 편입은 수중 보상 생길 때 보류.
- **게이트**: EditMode 전체 + Domain(밝기·재생 규칙) 테스트 + E2E(따기→소등→낫 진입→재생 / 배치·회수) + 아트 규칙(LightRule) 통과.
- **영역**: `World\`, `Harvesting\`, `Domain\`, `08.Data\`(아이템), `Testing\`

### 34. 범용 시간 제작 + 배터리 가공 (2026-08-05 사용자 확정)
- **모든 제작은 시간이 걸린다.** Job(레시피+소요시간+진행도) 코어를 Domain에 — 손 제작(이동 자유, 동시 1개, HUD 게이지, 취소=환급, 재료는 시작 시 소모), 제작대(작업이 스테이션 귀속 — 걸어두고 떠남), 화톳불 배터리 가공(스크랩→배터리, 불이 타야 진행, 스크랩당 25 충전 밸런스는 산출량으로 보존)이 전부 같은 코어를 쓴다. 미래의 화로·제작기는 스테이션 추가만으로.
- **데이터는 전부 ScriptableObject** — duration·가공 스테이션 지정을 RecipeSO 필드로(추후 인스펙터 수정 가능). 배터리 가공도 레시피 에셋 하나.
- **확장(2026-08-05, 러스트 참고)**: 동시 1개가 아니라 **제작 대기열** — 여러 작업 순차 처리, 수량 제작(N개 묶음), 항목별 취소 환급. UI는 러스트식: 인벤토리 하단 큐 슬롯(아이콘+수량+맨 앞 남은 시간), 닫아도 보이는 최소 HUD 표시. 스테이션(제작대·화톳불·미래 화로)도 같은 큐 모델.
- **게이트**: EditMode 전체 + Domain(Job 생애·환급) 테스트 + E2E(손 제작 시간·취소 환급 / 제작대 귀속·회수 / 화톳불 가공·불 꺼짐 정지) + 기존 제작 경유 E2E(GatherCraftBuild·BaseBuilding 등) 새 규칙 반영 개정.
- **영역**: `Domain\Crafting\`, `Crafting\`, `UI\CraftingUI`, `Building\Campfire`, `08.Data\Recipes\`, `Testing\`

## R10 예약 — R9 병합 후 투입 (Campfire·건축 데이터 충돌 방지)

### 35. 버섯 목재 벌목 (2026-08-05 사용자 확정)
- 거대 버섯 = 파괴형 채집 노드(곡괭이, 광맥 관례). 드롭 `버섯 목재`, 발광 개체면 빛도 소실(R9-B 체계 재사용), 그루터기 느린 재생.
- 소비처 3종(사용자 전부 선택): ① 건축 비용 재편(목재 중심+스크랩 소량 — 다리가 벌목을 요구) ② 화톳불·가공 연료를 **목재 전용으로 전환** ③ 레시피 재료 편입.
- **세계관 확정(2026-08-05)**: 스크랩은 연소재가 아니라 **에너지 저장 매체** — 불에 넣는 것이 아니라 열로 에너지를 꺼내는 대상(배터리 가공). 연료는 목재만. 목재 도입 전 초반 화톳불이 마비되지 않게 마이그레이션 순서 주의(섬1 필드에 버섯 나무 존재 확인됨).
- 밸런스 축: "다리 하나 = 거대 버섯 몇 그루" 실측 기준.
- **확장(2026-08-05)**: **곡괭이/도끼 구분** — 신규 도구 도끼(스크랩 손 제작, 목재 요구 금지=순환 방지). 벌목은 도끼만, 광맥·잔해는 곡괭이만(잘못된 도구 = 데미지 0 + 피드백). 노드에 요구 도구 필드, 판정 순수부 Domain. 사망 시 도구 비드롭 + 스크랩 재제작 가능이라 하드락 없음.
- **게이트**: EditMode 전체 + Domain 테스트 + E2E(벌목→드롭→재생 / 목재로 건축 / 목재 연료 가공) + 기존 건축·가공 E2E 개정 통과 + AutonomousGate.
- **영역**: `Harvesting\`, `World\`, `Building\`(비용 데이터), `Building\Campfire`, `08.Data\`, `Testing\` — **R9-C와 충돌, 순차 필수.**

### 36. 챕터 1 종막 재설계 — 포탈 대신 하강 (2026-08-05 사용자 확정)
- **챕터 1은 포탈 이동이 아니라 수면 아래 진한 매크로늄층을 뚫고 내려가며 끝난다.** 하강 수단 = **매크로늄으로 제작하는 잠항구**(섬4 자원의 용도 = 종막 열쇠, "같은 물질만이 진한 층을 통과한다"는 픽션).
- 구현: TraversalGear에 하강 종류 추가 + 진한 층 접촉 판정 확장(잠항구 보유 시 즉사 대신 통과·하강), 잠항구 RecipeSO(제작대, 매크로늄 요구), DescentZone + ChapterDirector 목표 체인 개편(포탈 기동 → 잠항구 제작·하강), 종막 연출은 기존 포탈 엔딩 흐름(SceneTransitionTrigger·ScreenFader) 재사용. 옛 종막 장치 오브젝트 제거는 §8-4 씬 작업에서(스폰 끼임 문제 동시 해소). → **✅ 완료 (2026-08-06, `auto/remove-portal`)** — 씬 오브젝트·컴포넌트·아이템·레시피·청사진·발견·FX·표의 줄까지 전부 걷어냈다. 재발 방지는 `RetiredContentGateTests`.
- **게이트**: EditMode 전체 + Domain(판정·목표) 테스트 + E2E — 잠항구 없이 진한 층 = 즉사 유지 / 잠항구 제작→하강→챕터 종료, E2EChapter1·Walkthrough 종막 구간 개정.
- **영역**: `Domain\World\`, `World\`, `Progression\`, `Narrative\`, `08.Data\Recipes\`, `Testing\` — **RecipeSO·RecipeBook이 R9-C(duration 필드)와 겹치므로 R9-C 병합 후 착수. R10-A(목재)와도 RecipeBook 공유 — 병합 시 코디네이터가 조정.**

## R10~R11 — 테크트리 (스펙: docs/superpowers/specs/2026-08-05-techtree-design.md)

### 37. 해금 상태 코어 + 현장 발견 채널 — ✅ 완료 (R10-B, 52ec1376)
### 38. 연구대 스테이션 — R11 예약
### 39. 연구 소재 공급 (유물 pity·부품 드롭) — R11 예약
### 43. 도감 UI — ✅ 완료 (R11-B, `auto/codex-ui`)
- 해금 원장이 이미 기록 중인 발견·청사진을 열람하는 화면. AI 분석 대사(성질 묘사)가 각 항목 설명문. 신규 데이터 불요 — UI만. 연구대 UI와 같은 계열로 묶어 작업.
- **구현**: 화면 제목 "분석 기록", **J 키**로 여닫는다. 씬을 고치지 않고 런타임에 캔버스를 세운다(`CodexUI` 자기설치 — `GammaCalibrationScreen` 관례). ESC는 스스로 듣지 않고 `UIStateService` 스택에 맡긴다(`UIPanelKind.Codex` 신설, 배타 규칙 없음 — 소지품 위에 겹쳐 뜨고 ESC 한 번에 이것만 걷힌다).
- **갈래 4종**: 물질 분석(DiscoverySO — 설명문은 그때 AI가 한 말 그대로) · 확보 제작법(BlueprintSO — 두 해금 채널의 unlocks를 훑어 모으고 열린 줄에는 출처를 적는다) · 정밀 분석(ResearchEntrySO — `ResearchService.IsKnown`과 같은 판정) · 개체 관측.
- **생물 페이지 열쇠 계약**: `codex_<CreatureDefinition.id>`. 이 열쇠를 **여는** 경로(관측·처치)는 별도 작업이고, 화면은 원장만 읽는다. 목록은 `CreatureBookSO`(`Resources/CreatureBook`) — 참조만 담은 카탈로그이며 이름·성향·수치·설명문은 전부 기존 `CreatureDefinitionSO` 필드에서 끌어온다.
- 목록을 짓는 규칙은 전부 순수부 `Domain/Progression/CodexCatalog.cs`에 있다(EditMode 20건). 미해금은 이름조차 주지 않는다(`???`).
### R11 잔여 배선: 진행 장비(잠항구·액면 보행·산소 상향) 청사진을 유물 연구에 연결, 목재·도끼 발견 매핑(disc_wood 등) 편입.

## ✅ 야간 자율 주행 결산 (2026-08-06 아침, main `386848bd`)

R10 잔여 + R11 전체 + 폴리싱 3건 완료. EditMode **814/814**, 자체 코드 경고 **0**.
- 완료: 포스트프로세싱 스위트(40)+랜턴 반사(41)+감마 화면(42) / 조준 회귀 수정 / 연구대(38) / 유물 pity 드롭·생물 부품·도감 열쇠(39) / 도감 UI(43) / CS0618 92→0 / 조준 최근접점화(적중률 +6~12%p, 잃음 0) / E2EConsumer 격리(44 — 오진 정정: 실원인은 스폰을 덮은 군락 밝은 구역)
- 남은 것: §8-4 씬 배치(사람과 함께 — 낫 서식지·짙은 층·DescentZone·균열·수중 보상·유도등·매크로늄 광맥·스폰 정리. ~~옛 종막 장치 제거~~ → 2026-08-06 완료) + "자율 실행 금지" 섹션의 사용자 판단 항목들. E2EChapter1/Walkthrough의 종막 구간은 낫 서식지 배치 전까지 원장 주입 우회 유지(주석 명시).

### 작업 사본 위생 정리 (2026-08-06, main `f1fe9b27`)

세 클론과 메인이 몇 주째 상시 "modified"를 달고 있던 원인을 갈라 정리했다.
- **줄바꿈 유령**: Unity는 YAML 자산을 항상 LF로 쓰는데 `core.autocrlf=true`가 CRLF로 체크아웃한다 → 에디터가 한 번 건드린 파일은 바이트까지 같은데도 영구 modified. `.gitattributes`에 Unity 자산 확장자 `text eol=lf` 추가. `eol=lf`는 체크아웃에만 걸리므로 기존 사본은 더러워지지 않음(확인 완료 — 신규 dirty 0건).
- **죽은 meta**: Feel의 `*.unitypackage.meta` 3개는 세 에디터가 독립적으로 지운 잔재 → 삭제 커밋.
- **`Assets/_Recovery/`**: Unity 크래시 복구 사본. `SurviveProject/.gitignore`에 추가(표준 `!/[Aa]ssets/**/*.meta` 되살리기 규칙보다 **뒤에** 와야 `.meta`까지 걸린다). 파일은 디스크에 남겨 둠.
- **`ChosunGu SDF.asset` (클론 B, +133자)**: 커밋하지 않고 되돌림. `m_AtlasPopulationMode: 1`(Dynamic) + `m_ClearDynamicDataOnBuild: 1` — 런타임에 다시 채워지고 빌드 때 비워지는 캐시라 커밋해도 이득 없이 매 세션 3천 줄 churn만 남는다. (기존 글자 손실 0건 확인.)
- 결과: 메인·클론 A/B/C 전부 `git status` 0항목, 모두 `f1fe9b27`로 동기.

### 40. 포스트프로세싱 스위트 (2026-08-05 방침 확정판)
- **대원칙: PP는 어둠을 만드는 게 아니라 부수지 않게 지킨다** (환경광 0으로 어둠은 이미 있다). 우선순위: ① **Fog(승부처)** — Exp², 깊이 밴드별 density·color로 "깊이=자홍 농도" 구현 + draw distance 성능 이득 ② Color Adj+SMH — 검정을 0으로, Shadows 자홍 쪽 ③ Bloom — threshold 높게, 광원만 HDR intensity>1 ④ Grain+Dithering(밴딩 방어) ⑤ Vignette 약하게 ⑥ **Tonemapping은 Neutral+커스텀 커브 (ACES 금지 — 4색 판독 보호)** ⑦ CA는 액면 근처 볼륨만. 의사 암순응은 범위 제외(추후 검토). DoF·모션블러 금지.
- **게이트**: 컴파일+EditMode+AutonomousGate + **암부 픽셀 불상승 검증**(효과별 before/after) + 상태·깊이별 스크린샷 세트 사용자 전달 + E2E 스모크 + 프레임 실측. **최종 수치는 사용자 눈 튜닝.**

- **추가(2026-08-05)**: SSAO 비활성(환경광 0 — 성능만 소모). **유도등 원칙**: 완전 어둠 구간엔 항상 원거리 광원 색 하나가 안개 너머로 읽혀야 함(코드 측: 밴드 안개가 유도 광원을 삼키지 않는지 검증 / 배치는 §8-4). **42. 감마 조절 화면**: 첫 실행 시 "무늬가 간신히 보일 때까지" 슬라이더 → PlayerPrefs 저장 → 기본 볼륨 post-exposure 적용. 모니터 편차가 난이도가 되는 것 방지 — 지금 넣어야 밸런싱을 두 번 안 함.

### 41. 랜턴-도구 반사 수정 (사용자 직접 요청)
- 랜턴 불빛이 플레이어와 너무 붙어 곡괭이 등 손 도구에 반사·과노출. 광원 앵커 재배치(어깨 뒤/위) 또는 손 도구 레이어를 랜턴 컬링 마스크에서 제외 — 조사 후 선택, 전후 스크린샷 증명.

### 44. E2EConsumer 플레이키 — ✅ 해소 (오진 정정)
- ~~야생 생물 간섭 추정~~ → **실원인: 발광 버섯 군락(반경 11m×3)의 밝은 구역이 시작 지점 둘레를 덮어**, 검사 위치에 따라 낫의 빛 회피가 발동. 격리 유틸(SleepWildCreatures/MuteAmbientLitZones/TryFindClearSpot/RestoreWorld) 신설로 5/5 통과.
- **배치 관찰(사용자 확인 가치)**: 스폰 주변 ~11m가 상시 밝은 구역 = 포식자가 시작 지점에 접근 불가라는 뜻 — §8-4 때 의도인지 확인.

## 자율 실행 금지 — 사용자 확인 필요

### R4-B(E2E 확장)에서 발견·기록된 사항 (2026-08-05)
- **E2EBaseBuilding 보관함 조준 실패** — 세운 storage가 (6,52,2)에 생기지만 PlayerInteractor가 4초 안에 조준하지 못함. **origin/main 순정에서 재현 확인된 선행 결함** (하네스 개선으로 도달이 빨라져 드러남). 원인 조사 필요 — 코드(조준 판정)인지 배치(콜라이더)인지 미확정 → 자율 조사 가능 항목.
- **걸어서 닿을 수 없는 배치**: LooseScrap_7 (10,51,10), LooseScrap_16 (16,51,13) — 레벨 배치 판단 필요.
- **캡슐이 박히는 지점**: (3.3, 51.4, 12.1) 부근 — 지형 수정 필요(사람 눈).
- ~~**스폰 정면이 막혀 있다 (R8 판별)**~~ → **해소 (2026-08-06)**: 스폰 (0,52,0)의 정면 +Z 1.63m에 옛 종막 장치의 솔리드 콜라이더가 서 있었다. 그 오브젝트를 씬에서 걷어냈다 — 이제 정면 첫 장애물은 1.93m의 버섯 무리다. Z≈-4의 `Debris_6`도 동일. 보행 E2E 산발 실패의 실원인(상호작용용 InteractBounds 트리거는 별도로 있으므로 본체 콜라이더를 trigger화하거나 스폰 위치·방향 조정 — 배치 판단 필요). 또한 **클론별 잔존 세이브가 시작 위치를 바꿔** E2E 재현성을 흔든다 — E2E 전 세이브 삭제 권장.
- **Trigger_Surveyed 중심 반경 3m가 포탈 콜라이더·턱으로 막힘** — 진행에는 지장 없음, 레벨 디자인 참고.
- E2EWalkthrough는 하네스 수정 후 목표 6까지 진행 — 남은 실패는 포탈 요구물 부족(재료 확률·배치)으로 별개 원인.

### R3에서 발견·기록된 사항 (2026-08-05)
- **E2EWalkthrough·E2EChapter1 기존 실패** — 첫 구간(`Trigger_Surveyed`행)에서 플레이어가 (0.4, 52.0, 1.8)↔(2.2, 52.6, 1.8) 사이를 진동하며 지형에 끼어 타임아웃. origin/main 순정 상태에서도 동일 재현(stash 검증) = 선행 결함. 지형(씬) 문제일 가능성이 높으나, R4에서 하네스 측 우회 가능성을 먼저 조사한다.
- **사망 후 부활·리스폰 부재** — 사망 처리 자체가 원래 없었고, 지금은 체력 0으로 계속 걸어 다닌다. 스폰 지점은 씬 데이터라 코드만으로 못 정한다. 왕복 압박이 실제로 성립하려면 필요 — 스펙 결정 필요.
- ~~드롭 가방은 세이브를 넘기지 않는다~~ → **해소 (R7-B)**: 사용자 결정으로 세이브 지속 구현 완료 (백로그 27).

- **지형 다듬기 (P2 §8-4)** — 문서가 명시적으로 "사람 눈 필요"로 분류.
- **광석 분포 보강** (CHAPTER1_LOG 후속 ③) — 미감·레벨 디자인 판단 필요. (같이 적혀 있던 ② "장치 위치 경사 조정"은 그 장치를 2026-08-06에 걷어내면서 사라졌다.)
- **미커밋 변경 처리** — `ChosunGu SDF.asset`(1310줄, GUID 파손 위험), `EditorSettings.asset`, `TimeManager.asset`(fixedDeltaTime는 물리·E2E 타이밍에 직결), `TestScene.unity`, `Plan/` 기획서 2건의 커밋/폐기 여부.
- **MainScene.unity·프리팹 직접 수정을 요하는 모든 작업** — 씬은 병합 불가 단일 파일이라 클론 병렬 작업과 근본적으로 충돌. **예외: 항목 18(UI 프리팹 분리)은 사용자가 직접 배정한 작업으로, 씬 배타 규칙 하에 단독 실행 허용.**

## 병렬 배정 예시 (충돌 없는 조합)

| 클론 | 작업 | 영역 |
|---|---|---|
| A | 1 (환경 위협 판정) | Domain 신규 + Tests 신규 |
| B | 2+3+4 (테스트 보강 묶음) | Tests 신규 파일만 |
| C | 7+8 (dead code 삭제) | Narrative\, Utill\ |

이후 라운드: A→9(1의 후속), B→17(UIManager), C→12 등. 11↔13(Player\), 10↔11(Testing\+Player 인접), 17↔18(UI 영역)은 같은 라운드에 배정 금지.

**18(UI 프리팹 분리) 라운드는 단독 편성** — 씬을 만지는 유일한 자율 작업이므로, 해당 라운드에는 다른 클론에 씬·프리팹 작업을 배정하지 않는다(코드·테스트 전용 작업과의 병렬은 가능).

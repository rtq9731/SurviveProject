# 인수인계 — 2026-08-04 새벽

작업을 여기서 멈췄다. **결정이 필요한 지점에 도달했고, 그 결정은 사람 것이다.**

---

## 1. 커밋된 것 (안전, 검증됨)

`4a716890` → `332cc448`, 커밋 13개. P0 아트 방향 확립 전 단계 + 최종 리뷰 수정 + 검사기 Light 확장.

- EditMode 112/112 통과
- 머티리얼 규칙 위반 20건 → 0건
- 라이트 규칙 위반 23건 **드러남** (색은 하나도 바꾸지 않았다 — 눈으로 판단할 일이다)
- 컴파일 에러 0, 끊긴 참조 0

여기까지는 전부 리뷰를 거쳤다.

---

## 2. 워킹 트리에 남은 것 (**검증 안 됨 — 커밋하지 말 것**)

최종 리뷰의 후속 4건을 작업하다 검증 단계에서 막혔다.

```
M  Assets/02.Scripts/Domain/Art/ArtPalette.cs          AllowedEmission을 불변으로
M  Assets/08.Data/Art/Volume_Islands.asset             의도 안 한 overrideState 해제
M  Assets/08.Data/Art/Volume_Surface.asset             동일 + White Balance 추가
M  Assets/09.Tests/EditMode/ArtPaletteTests.cs         API 변경 반영
M  Assets/09.Tests/EditMode/Survive.Tests.EditMode.asmdef
?? Assets/09.Tests/EditMode/SceneArtSettingsTests.cs   씬 설정 단언 테스트 (신규)
M  docs/superpowers/plans/2026-08-03-p0-verification.md
```

**테스트를 한 번도 돌리지 못했다.** 특히 위험한 것:
- `ArtPalette` API가 바뀌었는데 소비자(`MaterialRule`·`LightRule`)가 컴파일되는지 미확인
- Volume 프로파일 두 개를 다시 건드렸다 — 이 단계에서 **직렬화가 조용히 깨진 전례가 있다**(커밋 `84d91a55` 참조). 파일 원문을 읽어 확인해야 한다
- 신규 테스트 파일이 한 번도 실행된 적 없다

되돌리려면: `git checkout -- SurviveProject/Assets docs` 후 `SceneArtSettingsTests.cs*` 삭제. **단 아래 3절을 먼저 읽을 것.**

---

## 3. 막힌 이유 — 결정이 필요하다

**`SurviveProject/Assets/01.Scenes/StartScene.unity`가 워킹 트리에 수정 상태로 남아 있다.**

경위:
- P0 작업 중 Unity가 씬 내장 머티리얼을 재직렬화했다(`_EMISSION` 키워드 전파, `_Color` 구체화, `MOTIONVECTORS` 패스). 검토받지 않은 변경이라 디스크에서 되돌렸다
- 그런데 **Unity 에디터가 그 상태를 메모리에 들고 있다.** 저장할 때마다 다시 나온다
- 게다가 그 더티 상태에 **사람이 에디터에서 해둔 미저장 편집이 섞여 있을 수 있다.** 실제로 이 세션 초반에 그런 일이 한 번 있었다 — 카메라 near clip과 UI 앵커 두 줄이 그렇게 커밋에 섞였다(`eba298e0`)

그래서 이렇게 막힌다:
- 테스트 실행 도구가 미저장 씬이 있으면 거부한다 (정당한 가드다)
- `--save-before-run`을 쓰면 검토 안 된 재직렬화가 디스크에 박힌다
- 씬을 리로드해 더티를 지우면 **사람의 미저장 작업이 있었을 경우 영구히 사라진다**

**서브에이전트가 이 가드를 우회하려 했고, 다른 에이전트가 거부했다. 그 거부가 옳다.** 나도 우회하지 않았다.

### 사람이 답해야 할 것

> **StartScene을 Unity 에디터에서 열어놓고 뭔가 편집한 적이 있는가?**

- **없다** → 그냥 버려도 된다. Unity에서 StartScene을 다시 열거나(저장하지 않고), 에디터를 재시작하면 된다. 그다음 2절 작업을 검증하고 커밋하면 된다
- **있다 / 모르겠다** → 버리기 전에 Unity에서 직접 확인할 것. 필요하면 저장하고, 그 변경만 따로 커밋한 뒤 진행

---

## 4. 아침에 봐야 할 것 (P0 사람 게이트)

`docs/superpowers/plans/2026-08-03-p0-verification.md`에 답할 칸 넷이 비어 있다. 스크린샷 6장 경로도 그 안에 있다.

1. 랜턴 없이 초반 탐색이 가능한가
2. 밝은 곳이 특별하게 느껴지는가
3. 네 광원이 헷갈리지 않는가
4. 빛기둥에 실제 빛줄기를 넣을 것인가

**같이 볼 것**
- **StartScene이 거의 단색 주황이다.** 주황 환경광 × 주황 안개 × 주황 컬러필터 × 노출 +0.9가 겹쳤다. 바위·안개·모래폭풍이 색으로 구분되지 않는다. 고치지 않았다
- **라이트 23건을 다시 칠할 것인가.** 발광 버섯이 `#8CF2BF`인데 팔레트는 `#2FE6C8`, 화톳불이 `#FF9E47`인데 팔레트는 `#FF9A2E`다
- **`eba298e0`에 섞인 두 줄** — 카메라 `near clip 0.001 → 0.01`, UI 앵커 `-26 → -16`. 되돌리지 않았다

---

## 5. 다음 작업 후보

- **P1 건축 결함 셋** — `docs/superpowers/specs/2026-08-03-p1-building-gap-survey.md`. 토대 없이 벽을 들면 고스트가 월드 원점으로 순간이동하는 것이 제일 먼저다
- **라이트 23건 정리** — 검사기가 보고만 하고 아무도 고치지 않은 상태다. 콘텐츠를 더 얹기 전에 하는 게 싸다
- **2절의 후속 4건 검증·커밋** — 3절이 풀린 뒤에

---

## 6. 이 세션에서 배운 것 (반복하지 말 것)

- **에셋 검증은 파일 원문을 읽는다.** 인메모리 상태를 검증하고 디스크 상태를 커밋한 사고가 있었다
- **`components.Count == 4`는 널 슬롯 4개도 통과시킨다.** 개수가 아니라 `TryGet` 비널로 확인한다
- **패키지(`Packages/`) 내용을 우리 규칙에 맞추려 고치지 않는다.** git이 추적하지 않아 클론하면 사라지고, Unity도 경고한다
- **씬을 스크립트로 저장하면 에디터의 미저장 상태가 함께 박힌다.** 저장 전에 `git status`를 확인한다
- **EditMode만 돌리면 E2E가 깨진 것을 모른다.** 실제로 한 번 깨뜨렸다(`E2EBaseBuilding.cs:676`)
- **서브에이전트가 안전상 거부하면 다른 경로를 찾지 않는다.** 이 세션에서 세 번 있었고, 세 번째에야 멈췄다
